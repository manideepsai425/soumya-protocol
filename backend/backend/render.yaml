services:
  - type: web
    name: soumya-protocol-api
    runtime: python
    region: singapore          # closest to Telangana; change to oregon/frankfurt if you prefer
    plan: free                 # upgrade to starter ($7/mo) to eliminate cold-start delays
    rootDir: backend           # path relative to the repo root
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health

    envVars:
      - key: ALLOWED_ORIGINS
        # Replace with your actual Vercel URL once you have it.
        # You can also set this in the Render dashboard under Environment.
        value: https://soumya-protocol.vercel.app,http://localhost:5173
